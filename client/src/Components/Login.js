import React from "react";
import "./Login.css";
import { Button } from "react-bootstrap";
import FaceDetect from "./FaceDetect";

function Login() {
  // Placeholder code for login page
  // Used during setup of react-router

  return (
    <div className="App-header">
      {/*This is the login page*/}
      {/*<Button variant="primary" onClick={() => alert("Yes, this button works")}>*/}
      {/*  And this is a button from react-bootstrap*/}
      {/*</Button>*/}
    <div>
        <FaceDetect/>
    </div>
    </div>
  );
}

export default Login;
